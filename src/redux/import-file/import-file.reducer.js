const INITIAL_STATE = {
    error: null
}

const importFileReducer = (state = INITIAL_STATE, action) => {
    switch (action.type) {
        default:
            return state;
    }
}

export default importFileReducer;